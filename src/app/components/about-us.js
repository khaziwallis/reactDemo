import React from 'react';

//Stateless AboutUs component
//Stateless components does not maint state (this.state)
const AboutUs = () => {
    return (
        <div>
            <h1>About us</h1>
        </div>
    );
};

export default AboutUs;