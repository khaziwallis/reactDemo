import React from 'react';

//stateless componenet, does not need to maint state here, hence stateless
const ContactUs = () => {
    return (
        <div>
            Contact Us
        </div>
    );
};

export default ContactUs;